const path = require('path');

module.exports = {
  config: path.resolve('src', 'db-config.js'),      // use your actual config file
  modelsPath: path.resolve('models'),
  migrationsPath: path.resolve('migrations'),
  seedersPath: path.resolve('seeders')
};

